# Dependencies
node_modules/

# Environment variables (never commit credentials)
.env
*.env
.env.*
# ...but the template (no real secrets) IS committed:
!.env.example
!backend/.env.example

# OS files
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
npm-debug.log*

# Test output
coverage/

# Temporary / scratch files
test_*.js
verify_*.js

# Local dev/debug helpers — destructive or demo-cred bound, never run by the app.
# Kept on disk for local debugging but never shipped (see QA finding #3).
scripts/_*.js
scripts/cleanup-test-cashiers.js

# Local planning / build-guide docs (kept out of the repo)
celsopos_*.txt
