const getInventory = (req, res) => {
  res.status(200).json({ message: 'getInventory — not yet implemented' });
};

const updateStock = (req, res) => {
  res.status(200).json({ message: 'updateStock — not yet implemented' });
};

module.exports = { getInventory, updateStock };
