const getSales = (req, res) => {
  res.status(200).json({ message: 'getSales — not yet implemented' });
};

const createSale = (req, res) => {
  res.status(201).json({ message: 'createSale — not yet implemented' });
};

module.exports = { getSales, createSale };
